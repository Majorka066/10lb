export default function Header() {
  return (
    <header>
      <h1 className="header-title">📰 Новостная лента</h1>
      <p className="header-subtitle">React + API + компоненты + состояние</p>
    </header>
  );
}
